{
  "name": "examples",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild ts/*.ts --bundle --minify --outdir=built --sourcemap",
    "serve": "http-server"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gl-handler": "^1.0.0",
    "gl-matrix": "^3.4.3"
  },
  "devDependencies": {
    "esbuild": "^0.14.10"
  }
}
